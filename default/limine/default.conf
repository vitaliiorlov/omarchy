# [omarchy] Original template used TARGET_OS_NAME, UKI, and snapshot settings
# CachyOS manages /etc/default/limine — this file is kept as reference only

ESP_PATH="/boot"

BOOT_ORDER="*, *lts, *fallback"

FIND_BOOTLOADERS=yes
