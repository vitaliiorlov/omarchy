#!/bin/bash
STATE_FILE=~/.local/state/omarchy/toggles/lgtv-auto-power-off

if [[ -f "$STATE_FILE" ]]; then
  rm -f "$STATE_FILE"
  notify-send "󰐝    LG TV auto power enabled"
else
  mkdir -p "$(dirname "$STATE_FILE")"
  touch "$STATE_FILE"
  notify-send "󰐝    LG TV auto power disabled"
fi
